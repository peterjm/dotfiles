#!/bin/bash

CURRENT_BRANCH=`git branch | awk '/^\*/ { print $2 }'`
if [ "$CURRENT_BRANCH" == "master" ]; then
  git pull
else
  git checkout master
  git pull
  git checkout $CURRENT_BRANCH
  git rebase master
fi
