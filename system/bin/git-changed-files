#!/bin/bash

if [ "$1" == "--include-branch-commits" ] || [ "$1" == "-b" ]; then
  git diff --name-only $(git merge-base HEAD $(git-main-branch))
  git ls-files -o --exclude-standard
else
  git diff --name-only
  git ls-files -o --exclude-standard
fi
